# v4.0.0 正式发布冻结清单

- 发布包 ID：`anchor-v4-formal-20260828`
- 版本：`v4.0.0`
- 发布日期：`2026-08-28`
- 源候选：`scorer/anchor_v4_全量19指标_候选.md`（只读；本次未修改）
- 发布前提：`anchor_v4-heldout-v2` 的 152/152 单元、19/19 指标已完成独立 reviewer PASS；batch08 由 Director 验收（上游任务 `t_3551d63f`）。

## 冻结项目

- [x] 新增版本化发布目录；不覆盖候选、v3、held-out、corpus、split 或任何历史评分结果。
- [x] 固定模型/provider：`minimax-cn/MiniMax-M3`；不使用 `minimax` provider。
- [x] 固定指标集合和顺序（恰好19项）：`B01、B02、B03、C01、B08、B09、B16、B23、B30、B34、B36、C22、N3、N6、B31、B33、C14、B18、N7`。
- [x] 每项仅允许 `0/5/10`；19项规则正文含定义、可观察证据、判定标准、阈值、边界示例和流程。
- [x] 固定正文口径：`L=len(re.sub(r"\s+", "", text))`、`K=L/1000`；Python Unicode 码点，去全部空白、保留标点。
- [x] 固定首行规则：构建侧删除首个物理文本行及其行终止符；删除内容及标题映射不进入 worker；异常材料拒绝并替换。
- [x] 固定评分 JSONL schema：顶层仅 `id/rep/scores`，scores 恰好19项且 exact order；每项仅 indicator/score/evidence/reason；编码、原始长度、`L`、`K`、统计脚本/命令和首行剥离审计统一由构建侧受限 sidecar 承载，不进入评分 JSONL。
- [x] 固定构建侧输入审计 sidecar：由 `构建侧输入审计.schema.json` 与 `构建侧输入审计模板.md` 约束，明确 UTF-8、原始长度、`L`、`K`、统计脚本/命令及首行剥离字段；sidecar 不挂载给评分 worker。
- [x] 固定 N6 阈值已从候选状态升为正式冻结：F<0.20、0.20≤F<0.50、F≥0.50 对应 0/5/10；其他规则正文阈值同样冻结。
- [x] 发布包不包含正文、商业标签、作者/标题映射、gold、历史评分或抽样台账。
- [x] 发布前后逐项变更登记已完成；无未登记的指标定义、阈值、证据要求、顺序或评分路径实质变更。
- [x] hash 清单在全部 payload 文件写入后生成；hash 清单明确声明自身不纳入 payload digest，避免自引用。

## 下游闸门

发布包 reviewer 必须独立复算全部 payload hash、19项集合与顺序、schema 结构、首行/字符口径、无正文/标签/标题映射，并核对逐项变更登记；reviewer PASS 后仍须 Director 对 fresh50 抽样及正式评分另行授权。
