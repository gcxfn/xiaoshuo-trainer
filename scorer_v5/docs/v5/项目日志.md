>
> 本文件为项目唯一日志。每次状态、流程、约束或结论变更后必须同步维护。
> 纪律：不得编造执行、测试或验收结果；统计结论必须带数值和样本量；不得修改冻结产物或静默改变已授权任务契约。
>
## 2026-08-29 17:20（UTC+8）· Step 12.5 启动：七档正式确定 + draft-3 快照冻结

### 触发
用户下发《Step 12.5：Dev10 前评分器修正计划》：统一七档（0/1/3/5/7/9/10）、统一 AI 输出格式（prompt=validator=extractor 同一 JSON Schema）、修 Python 确定性逻辑、实现单一规则源、加强 validator、重做 preflight、补齐测试后再进 Dev10。

### 决策
- **七档正式启用**：19 指标 `score_scale.allowed` 全七档，`ordinal_mapping` 每个指标都必须产出 1/3/7/9 中间档（消灭"文件写七档、程序只出 0/5/10"）。
- **冻结断点不动**：v4.0.0 的 0/10 分支语义逐字保留（N6 F<0.20=0、F≥0.50=10；B33 F<0.60=0、F≥1.20=10；B01 无事件或 q>10%=0、q≤10% 且 ①②③=10 等）。中间档只在冻结 5 分支（和 10 分支内部）单调细分，四轮复核的等价性资产不丢。
- **draft-3 快照冻结**：`scorer_v5/specs_snapshot_draft3/`（19 YAML + 逐文件 sha256 清单），升版前基线，供审计比对。
- 连续原始值（F/N/q/比例）始终保留在 continuous 字段。

### 影响
- 16:20 门禁"中间档不进入正式评分"被 Step 12.5 授权更新为"七档正式确定"；本条目即登记，旧结论保留在 16:20 条目供追溯。
- 规格版本：spec-v5.1-draft-3 → **spec-v5.1-draft-4**（七档）。

### 更正（18:10）
- 本条目 17:20 决策节对 N6/B33 的 10 分支表述（"F≥0.50=10 / F≥1.20=10"）是草案级简化；draft-3 快照实际为全七档：N6 断点 0.20/0.30/0.40/0.50/0.65/0.80（F≥0.80=10）、B33 断点 0.60/0.75/0.90/1.20/1.50/1.80（F≥1.80=10）。
- 迁移脚本最初误按草案简化实现（N6/B33 只到 0/1/3/5/10），违反"冻结断点不动"；**已恢复快照断点**，N6/B33 现为全七档且与 `specs_snapshot_draft3/` 一致（validate_specs PASS，smoke PASS）。此为登记，非静默变更。
- 冻结 0/10 分支语义不变：N6 F<0.20=0、B33 F<0.60=0 仍为 0 分支；10 分支内部按快照断点细分 7/9/10。
- **B18 同步恢复（18:20）**：迁移脚本自创的中间断点 0.40/0.10（5 分支拆 5/7、10 分支拆 9/10）违反"冻结断点不动"，已撤销；恢复快照断点 `≥50% 或连续≥3 → 0；[25%,50%) → 5；<25% → 10`，allowed={0,5,10}（与快照映射一致）。
- 其余 16 指标：快照映射为 {0,5,10}（allowed 满七档是 draft-3 遗留），Step 12.5 授权在冻结 5/10 分支内单调细分出 1/3/5/7（如 B08 1 项=3、2 项=5），mapping_note 逐指标写明，非漂移。

### 二次修订（18:40）· 用户裁定：全部 19 项必须完整七档
- 独立 reviewer 二轮审核指出"仅 N6/B33 真七档"，用户 ask 明确选择「全部 19 项必须完整七档」（选项写明需新断点）。
- **裁决登记**：这是规格级重设计，覆盖 AGENTS.md「每指标可只用子集」旧约束。计数类指标（B08/B09/B23/B30/B36/C01/B16/C22/N7/B31/B34/C14）为凑齐 1/3/5/7/9/10，在冻结 0/10 分支间新增单调细分断点（如 B08 用 action_chain_count 极值 8 切 9/10；B09 用 idle_gap_ratio≤10% 切 10）。**这些断点无 v4 冻结依据，是用户裁定下的新设计**，逐指标 mapping_note 标注"Step 12.5 二次修订：用户裁定全七档"。
- 约束更新：AGENTS.md 固定约束"统一允许评分集合"相应修订为"全 19 项 allowed=[0,1,3,5,7,9,10]（用户裁定）"。

### 五项修复完成（19:10）· 独立 reviewer 二轮审核意见全部落实
- **① 全 19 项完整七档**：B01/B02/B03/C01/B08/B09/B23/B30/B36/N7/B31/B34/C14/C22/N3/B16 全部补齐 1/3/5/7/9/10（N6/B33 已是）。计数类用强度字段细分（B08 action_chain_count、B09 idle_gap_ratio、B23 chain_length、B30 event_count、B36 axis_count、N7 plot_count、B01 q、B02 position_ratio、B03 independent_secret_count、C01 max_quote_offset、C14 x_max_ratio、C22 subplot_count、N3 extreme_word_count/peak_all_irreversible、B16 last_5pct/callback_latest、B31 round_count、B34 entity_count、B18 invalid_ratio 连续细分）。逐指标 mapping_note 标注"用户裁定全七档"。
- **② B09 zero_sum 类型修复**：spec 描述加"（bool）" → schema 推断 boolean，extractor `is True` 判定生效。
- **③ N6/B33/C14 关键修复**：N6 按 game_goal 分组合并；B33 终局事件（末 15% 区间）不计入 N；C14 A/B 限焦点角色、C 检查 c_inner 本体的 ≥51 码点、y_flag 白名单（'无'/空不触发）+ schema 允许 null、character 允许 null。
- **④ Preflight 逐 spec 版本检查**：spec_versions 必须统一且与 config.metric_spec_version 一致（曾测出 11 draft-4 + 8 draft-3 仍 PASS 的漏洞已封堵）。
- **⑤ 首批 6 指标边界测试**：`test_dev10_six.py`（B01/B02/N6/B33/C14/B36 × 低/中/高/边界/典型错误），长文本（L=2104）使 q/position/F 断点可达，全 PASS。
- 附带修复：extractor `_locate` 捕获歧义引文 ValueError（不再崩溃）；output_schema `a_evidence` dict 类型、`（若有` 触发 null、y_flag null。
- 全量验证：smoke_pipeline / smoke_preflight / test_fact_layer / test_contract / test_dev10_six / validate_specs 全 PASS。

### B33 终局排除改为语义终局（19:40）· 三审前 spec-代码对齐
- 审核指出：B33 spec 写"终局=最后一个改变主线归属/结果的冲突目标场景；开放结局不排除"，代码却无条件排除末 15%——spec-代码漂移（文档说 X 实际做 Y）。
- 修复（方案①）：`semantic_outputs` 新增顶层 `finale_quote`（LLM 输出语义终局引文，开放结局/无明确归属为 null）；extractor 定位 finale_quote → finale_start，事件 start ≥ finale_start 才排除；finale_quote=null 不排除任何事件。
- 附带：`_locate` 处理空/None 引文返回 None（不再崩溃）；output_schema null 推断补"为 null/时为 null"。
- B33 spec 全量重建（edit 工具 splice 损坏后 write 恢复）：完整 semantic_definition + finale_quote + 更新 derived_features（finale_start/is_finale）+ evidence_rule + 七档映射保留。
- dev10 测试 B33 5 case 全部带 finale_quote，语义终局验证 PASS。
- 全量验证：6 测试入口全 PASS。

### 三审意见落实（20:10）：schema 漏字段拦截 + C14/B36 bug + provenance 模型记录
- **schema 漏字段必须拦住**：`_check_node` object 分支补 required 检查（数组项漏关键字段此前不报，如 B01 漏 event_quote、B36 漏 axis_quote 曾 parse OK）；`_is_optional_field` 把可选字段（nearby_anchor、条件字段"…时必填"、含"可选/若有/为 null"）排除出 required（防合法输出被误拒）。实测：漏 event_quote → FAIL_SCHEMA；仅缺可选字段 → OK。
- **C14 bug**：x1 块（非焦点直接内心）补 Python 校验 character≠focal（此前只信 LLM 的 x1_nonfocal 标记，可能假否决）。
- **B36 bug**：breadth_flag 改用已定位轴（axis_entries）而非 seen（此前定位失败的轴也计入③，违反"Python 校验引文定位"）。
- **N6/B33**：核对无确认算错的逻辑（game_goal 分组、语义终局已在前轮修好）。
- **provenance 模型记录**：ProvenanceRecord 新增 `model_params`（temperature/top_p/seed 从冻结 config 读）；run_card 的 `raw_stdout_hash` 从恒 None 改为记录 raw 输出的 sha256。每条记录含 provider/model/model_params/prompt_hash/spec_hash/text_hash/sidecar_hash/raw_stdout_hash/parsed/validator/final_score/timestamp——"这分是谁打的"可追溯。
- 全量验证：6 测试入口全 PASS。

---

## 2026-08-29 17:00（UTC+8）· 第二阶段 Step 5–13：Python 确定性评分栈落地

### 触发
Step 5 前门（16:20，v5.1-draft-3 阈值等价复核）PASS 后，按计划推进 Step 5（文本事实层）→ Step 6（删 LLM score）→ Step 7（N6/B33 事件结构）→ Step 8（criterion 模式）→ Step 9（ABSTAIN）→ Step 10（formal 禁自动修 JSON）→ Step 11（严格 validator）→ Step 12（固定模型环境）→ Step 13（完整 provenance）。

### 交付（scorer_v5/ 全部新增）
- **preprocessing/**：`canonicalize.py`（NFC+BOM+CRLF 归一，`L=len(re.sub(r"\s+","",text))` 口径）+ `offsets.py`（逐字引文定位，歧义拒绝，邻近锚点跨段感知）+ `paragraphs.py`（自然段坐标）+ `windows.py`（10%/15%/33%/85% 边界，85% 用 ceil 保证末 15% 窗口不短）+ `sidecar.py`（L/K/paragraph/边界 + sha256）。
- **runtime/**：`specs.py`（YAML 唯一规则源只读加载 + 实时 hash manifest）+ `parsing.py`（formal 模式零修复：FAIL_PARSE / FAIL_DUPLICATE_KEY，duplicate key 直接 FAIL 不 last-write-wins）+ `prompts.py`（spec 驱动 prompt，LLM 只输出语义+逐字引文，严禁 L/K/F/offset/score）+ `validation.py`（V1 schema / V2 evidence 逐字定位可复算 / V3 语义逻辑（qualifies=true 必须带有效证据）/ V4 score 由程序重算）。
- **scoring/**：`derived.py`（派生特征上下文）+ `extractors.py`（19 指标各自实现冻结 derived_features：B02/B31 round_count≥4 两完整交换、N6/B33 区间合并计数 N、B09 最大连续空档、B34 回退选场链、B36 六轴+两主线场景、N3 峰值并列、C14 焦点角色场景数→码点→首次、B18 中段分母+≥50/≥25、N7 字典序并列留档）+ `ordinal.py`（冻结 ordinal_mapping 谓词执行器，未知键硬失败）+ `engine.py`（ABSTAIN 不转 0，validation 失败不出分）+ `stats.py`（raw_valid/schema/duplicate/evidence/abstain 五率）。
- **config/formal_model.yaml**：正式 route 冻结 minimax-cn/MiniMax-M3，temperature=0.0、top_p=1.0、seed=0、seed_supported=true、prompt_version=v5.1-prompt-1、metric_spec_version=spec-v5.1-draft-3；`preflight.py` 五链校验（spec_hash→prompt_hash→scoring code version→model config→canonical text hash），任一不一致禁止启动。
- **provenance/registry.py**：14 字段溯源（run_id/book_id/metric_id/rep/model/provider/prompt_hash/spec_hash/text_hash/sidecar_hash/raw_stdout_hash/parsed_output/validator_result/final_score/timestamp）。
- **run_card.py**：单卡正式 runner（sidecar→prompt→parse→validate→derive→score→provenance），供逐卡 staging 使用。
- **tests/**：`test_fact_layer.py`（边界单测：10/15/33/85% 切点、歧义拒绝、K=L/1000、段落坐标、锚点跨段感知）+ `smoke_pipeline.py`（B02 两交换=10、单交换=0、N6 F=13.5 落档 10、ABSTAIN 无分、FAIL_PARSE、FAIL_DUPLICATE_KEY）+ `smoke_preflight.py`（preflight 通过/配置版本不匹配拒绝、provenance 往返、spec_hashes 一致性）。

### 校验（全部实跑 PASS）
- `python scorer_v5/tests/test_fact_layer.py` → ALL PASS
- `python scorer_v5/tests/smoke_pipeline.py` → ALL PASS（B02=10 / B02 short=0 / N6=10 / ABSTAIN / FAIL_PARSE / FAIL_DUPLICATE_KEY）
- `python scorer_v5/tests/smoke_preflight.py` → ALL PASS（preflight 通过 + 配置版本不匹配拒绝 + provenance 1 条往返 + spec_hashes 0 mismatch）
- `python scorer_v5/scripts/validate_specs.py` → PASS（19 文件）
- `python scorer_v5/run_card.py`（CLI 端到端）→ B02=10，provenance 四 hash 齐全

### 设计要点
- **LLM 永远不输出正式 score**：v4.0 的 reason-score 矛盾（36/2850）结构性消灭；score 只由 semantic facts + sidecar + 冻结 ordinal_mapping 生成（V4）。
- **formal 模式零修复**：duplicate key / 解析失败直接 FAIL，统计 raw_valid_json_rate 等五率；_smart_fix 类路径不存在。
- **ABSTAIN 是独立状态**：不自动转 0，单独统计 abstention_rate；大量 ABSTAIN 本身就是指标不可操作的证据。
- **中间档细分（1/3/7/9）不进入正式评分**：当前 ordinal_mapping 全部恢复 0/5/10 冻结分支；连续原始值（F/N/q/比例）始终保留在 continuous 字段，待 Dev10/Cross-model 验证后再决定启用。

### 下一步
- Step 14–15：从 Development 10 本选 6 个代表指标，每本×每指标 3 次（180 调用）→ Dev10 实验。

> 本文件为项目唯一日志。每次状态、流程、约束或结论变更后必须同步维护。
> 纪律：不得编造执行、测试或验收结果；统计结论必须带数值和样本量；不得修改冻结产物或静默改变已授权任务契约。

## 2026-08-29 16:20（UTC+8）· 第二阶段 Step 5 前门：v5.1 阈值/档位等价复核修复（spec-v5.1-draft-3）

### 触发
项目日志 15:40 待办：v5.1 ordinal_mapping 中间档细分为临时设计，须在 Step 5 前由 reviewer 复核阈值是否仍尊重 v4.0.0 冻结边界语义。独立 reviewer 连续两轮发现 19/19 与 11/19 档位语义未保持冻结边界。

### 复核结论（独立 reviewer，4 轮）
- **首轮 FAIL（19/19 语义不一致）**：v5.1-draft-1 的中间档细分（1/3/7/9）把 v4.0.0 冻结的 0 分支/5 分支改成正分（如 C01/B08/B09/B23/B30/B36 的 1 项命中从 0→3；N6 的 F∈[0.10,0.20) 从 0→1/3；B31 的 1 项从 0→3；B18 的 5%–25% 从 10→5/7；B33 的冻结 5 档无 v5=5 等）。
- **修复 1（spec-v5.1-draft-2）**：19 个 YAML 的 ordinal_mapping 恢复 v4.0.0 冻结 0/5/10 分支。
- **第二轮 FAIL（11/19）**：ordinal 数字壳恢复，但分支输入（semantic_outputs/derived_features）与冻结定义不一致——B02 回合不完整仍可得 5/10；B16 否决语义被改成 C22 的巧合/悔悟；B30 宣泄块限定结尾附近并漏"至少两起事实承载"；B34 当众性排除主角对手、不可逆代价改成主角方沉没成本；B36 六轴被换成题材/套路轴且漏"两个主线场景"核心驱动；C22 支线闭合从 ≥2 条缩成 1 条、④缺"含交锋回合和不可逆决定"；N3 词表漏 歇斯底里/狂喜/欣喜若狂 且④改成身份/动机收敛；B31 缺 item1/item2 布尔输入且并列取首次；C14 的 C/X/Y 定义漂移；B18 端点/分母/阈值语义漂移（>50% vs ≥50%）；N7 载体/跨场景/损失语义漂移。
- **修复 2（spec-v5.1-draft-3）**：11 个 FAIL 指标的 semantic_outputs/derived_features/mapping_note 全部恢复 v4.0.0 冻结定义（含 B02/B31 的 round_count ≥4 两完整交换、B16 V1=元话语 V2=另起无关新事件、B30 全文克制+两起事实承载、B34 回退选场链、B36 六轴+两主线场景、C22 V1 双分支（>200 演讲块或求赞/元话语，后者不受 200 限制）+≥2 支线+④含交锋回合与不可逆决定、N3 峰值并列候选+冻结词表+主角本体收敛、C14 冻结 C/X/Y、B18 ≥50/≥25 精确阈值+端点定义、N7 C1–C5 载体+同一假信息两场景+实质损失+字典序并列留档）。
- **第三轮（2 FAIL）**：B34 缺"无不可逆结果场→最后一个可确定主线结果的实时交锋场"回退链；N3 只有单峰值对象，无法表示并列候选。
- **修复 3（spec-v5.1-draft-3 续）**：B34 补回退链（最后不可逆确定结果场→最后一个可确定主线结果的实时交锋场→无清算场）；N3 改为 peaks 候选清单 + Python 三序排序保留并列 + 每并列候选独立窗口计数取最高档路径。
- **第四轮 PASS（19/19）**：全部指标 0/5/10 分支语义与 v4.0.0 冻结规则等价（total + 互斥 + 分支输入等价）。

### 产物
- 19/19 YAML 升级 `spec-v5.1-draft-3`（ordinal_mapping 恢复冻结 0/5/10；11 个指标 semantic_outputs/derived_features 恢复冻结定义）。
- `specs/README.md` 版本规则更新（当前 spec-v5.1-draft-3，变更必须升 draft-4 并登记）。
- `spec_hashes.json` 重新生成（spec-v5.1-draft-3）。

### 校验
`python scorer_v5/scripts/validate_specs.py` → PASS（19 文件，全部 v5.1 结构）。档位等价由独立 reviewer 四轮复核（结论：PASS）。

### 影响
- Step 5 前置门通过，可开始 Step 5（Python 统一文本事实层）。
- 中间档细分（1/3/7/9）在没有任何实证验证前不进入正式评分；正式评分仍按 0/5/10 落档。连续原始值（F/N/q/比例）始终另外保存，待 Dev10/Cross-model 验证后再决定是否启用细分档。

---

## 2026-08-29 15:40（UTC+8）· 第二阶段 Step 4.5：v5.1 规格收口

### 触发
用户检查整个项目后指出：当前实现仍基于"修改前"的 v5 计划。Step 1–4 不用推倒重来，但 Step 5 前必须插入 Step 4.5 完成 v5.1 接口收口。

### 用户 8 点要求 → 全部落实

| # | 要求 | 落实 |
|---|---|---|
| 1 | 全项目同步 0/1/3/5/7/9/10 七档制 | 19/19 YAML `score_scale.allowed` 改为七档子集（不强迫每指标全七档；连续型保存原始连续值再映射） |
| 2 | N6/B33 不该让 LLM 报 start_offset/end_offset；B01/B02 删 LLM 记 P/码点位置 | N6/B33 的 LLM 只报 round_quotes + nearby_anchor，Python 定位 offset；B01/B02 的 llm_tasks 删除 P/码点记录 |
| 3 | deterministic_inputs 里混入 LLM 语义判断（B36 core_drive/breadth、B23 finale_initiative、B30 escalation、B09 stake_level） | 拆成三层：semantic_outputs（LLM 判断）/ derived_features（Python 计算）/ ordinal_mapping（档位） |
| 4 | C14 "LLM 做选择、Python 又做选择"职责重复 | C14 焦点角色只由 Python 排序选出（场景数→码点→首次），LLM 只标块归属；N3 窗口/词表计数完全 Python |
| 5 | "19 项无 SUBJECTIVE"下结论过早 | 修正为"19 项均已尝试操作化，暂不预先归为 SUBJECTIVE"；B09/B36/C22 等标注"待验证" |
| 6 | validate_specs.py PASS 只是结构 PASS | 加强：拒绝旧 0/5/10、拒绝旧字段（llm_tasks/deterministic_inputs/score_rule）、LLM 输出含 L/K/F/offset 拒绝、N6/B33 缺事件结构拒绝、版本/hash 检查 |
| 7 | 唯一规则源 + Formal Preflight 未进入项目 | 写入 AGENTS.md 固定约束：specs/*.yaml 是唯一 active rule source；启动前检查 spec_hash→prompt_hash→code version→model config→canonical text hash |
| 8 | Dev10 "包含 G2 高分歧"不严谨 | 改为"覆盖结构压力案例（高计数压力/长文/短文/不同档位）"；可另加 diagnostic cases（不纳入 Dev10 科学结论） |

### 产物
- 19/19 YAML 升级 `spec-v5.1-draft-1`（三层结构 + 七档制 + LLM 不报 offset）
- `schema.json` 更新（新结构，拒绝旧字段）
- `validate_specs.py` 加强（PASS：19 文件）
- `step4_拆解表.md` 更新（step4-v5.1-draft-1：暂不预先归为 SUBJECTIVE + 待验证项标注）
- `specs/README.md` 更新（v5.1 结构说明 + 唯一规则源声明）
- Dev10 登记修正（`旧数据重新分类登记_v1.md`）
- `spec_hashes.json` 生成（Formal Preflight 用）

### 校验
`python scorer_v5/scripts/validate_specs.py` → PASS（19 文件，全部 v5.1 结构，旧字段/旧三档自动拒绝）。

### 下一步
- Step 5：Python 统一文本事实层（canonical text / sidecar）。
- 注意：v5.1 的 ordinal_mapping 中间档细分（如 N6 的 0.10/0.15/0.35/0.50 断点）为**临时设计**，须在 Step 5 前由 reviewer 复核阈值是否仍尊重 v4.0.0 冻结边界语义（已于 16:20 完成，见上方 Step 5 前门条目）。

---

## 2026-08-29 15:10（UTC+8）· 第二阶段 Step 4：逐个拆解 19 指标

### 执行内容
- 产出 `scorer_v5/docs/step4_拆解表.md`（step4-draft-1）：19 指标逐判定项回答问题 A/B/C。
- **结论**：
  1. 全部 19 指标的 L/K/比例/阈值/0-5-10 映射 → **Python**（计划 Step 5 事实层）；
  2. 全部语义判定（有效冲突/关系升级/身份反转/同一事件/状态改变/同一场景）→ **LLM**；
  3. **无 SUBJECTIVE 指标**（19 指标全部可拆为 LLM 语义判定 + Python 确定性计算）；
  4. **N 计数权裁决（N6/B33）**：LLM 输出结构化事件清单（start_offset/end_offset/回合引文/解码证据/博弈目标），**Python 合并去重后计数 N**——根治 v4.0"LLM 自估 K/N"问题（158/283 条）；
  5. 混合项模式（LLM 标候选 + Python 算比例/计数/排序）为 v5 通用执行模式。

### 规格升级（spec-draft-1 → spec-draft-2）
- **N6.yaml / B33.yaml**：升 spec-draft-2——新增 `event_schema`（机器可校验字段），N 定义改为"Python 从事件清单计数"；required_evidence 字符串字面量坏形态一并修复。
- schema.json + validate_specs.py 同步支持 `event_schema`。
- 校验：`python scorer_v5/scripts/validate_specs.py` → PASS（19 文件）。

### 待办（Step 5 起）
- Step 5 Python 统一文本事实层（L/K/offset/paragraph/比例 sidecar）。
- Step 6 删 LLM score（N6/B33 事件结构就是该步骤的落地载体）。
- Step 7 重构 N6/B33（优先做）。

---

## 2026-08-29 14:55（UTC+8）· Step 3 修订（依据 reviewer 复核，PASS with conditions）

### 复核结论
- **阈值/判定项/档位语义与 anchor 全部逐字一致**（B01 10.00%、B02 500/15%、B09 30%、B16 15%、B18 50/25/≥3、B30 ≥51/≤2、B33 0.60/1.20、N6 0.20/0.50、C14 30%/70%、C01 500、N7 C1-C5、N3 200 窗口）。
- **冻结源 hash 完好**：`scorer_frozen/release_v4/` 7 文件 sha256 全部匹配发布包 SHA256SUMS.json。
- PASS（有条件），修 3 项必改 + 2 项建议后通过。

### 修订内容
1. **B03 deterministic_inputs 恢复单键 dict 形态**（原被写成字符串字面量 `- '- secret_count: ...'`）。
2. **C14 deterministic_inputs 也修复为单键 dict**（原 3 项字符串）+ required_evidence 字符串字面量修复。
3. **N3 补 `extreme_word_list`**：固定极限词表（嘶吼/撕心裂肺/目眦欲裂/崩溃/痛不欲生/嚎啕大哭/歇斯底里/绝望/狂喜/欣喜若狂，逐字复制自 anchor §13）。
4. **README 修正**：路径引用 `scorer/release_v4/` → `scorer_frozen/release_v4/`；"逐字提取"表述改为准确说法（semantic_definition 为压缩改写、score_rule/阈值逐字）；新增"固定顺序"节明确顺序是内容属性、与文件系统排序无关。
5. **新增 `scorer_v5/specs/schema.json`**（统一 schema）+ **`scorer_v5/scripts/validate_specs.py`**（校验脚本入仓）。

### 校验（脚本实跑）
- `python scorer_v5/scripts/validate_specs.py` → PASS（19 文件、10 必填字段、deterministic 单键 dict、N3 词表）。
- 阈值抽查 12 项全部 in-anchor & in-spec。
- 冻结源 7 文件 hash 全部 OK。

### Step 4 前待决策（reviewer 指出，不阻塞本步）
- **N 计数权归属**：N6/B33 的 `F=N/K` 中 N 仍是"LLM 判定计数，Python 校验结构"。v4.0 的 K 自估问题（L/K 进 deterministic）已修，但 **N 的计数权仍在 LLM**——这是"LLM 无法可靠取得 K 却自行估计"的姊妹问题。Step 4 必须定义 N 的机器可校验接口（每件位置区间、A 代码、闭环标记）。
- **deterministic_inputs 里 7 处纯语义"判定"项**（B08.causal_autonomy、B09.stake_level、B23.finale_initiative、B30.escalation、B36.core_drive/breadth、N7.loop_complete）实为"LLM 判定后 Python 校验"，不是"Python 确定性计算"。Step 4 拆解时决定去留，若推翻须升版本登记。

---

## 2026-08-29 14:43（UTC+8）· 文件夹整理（过时归 archive，最新计划归 docs/v5）

### 执行内容
- 根目录 29 项 → **9 项**：
  - 保留：`.hermes` / `AGENTS.md` / `corpus`（只读语料）/ `data`（切分特征）/ `判爆扑台账` / `全流程计划.md`（历史流程定义）/ `docs`（唯一活跃文档）/ `scorer_v5`（唯一活跃代码）/ `scorer_frozen`（冻结参照）/ `archive`（全部历史）
- **最新计划文件夹 `docs/v5/`**：`小说商业规律研究与评分器重构完整实施计划 v5.md` + `项目日志.md` + `旧数据重新分类登记_v1.md`
- **归档 `archive/`**（只读审计）：
  - 全部 v3/v4 实验目录：anchor_v4_heldout{,_exec,_v2,_v3}、blind_scoring_v4、challenge_set_v1、fresh50_{review,sampling,sampling_v2,scoring,scoring_v2}_v1/v2、g2_stability_v1/v2、review-batch02、盲评v3_1
  - 旧评分器：`archive/scorer_old/`（BAT1-6、anchor_v2/v3/v4 候选、smoke 测试）
  - 旧文档：`archive/docs_old/`（全部 v3/v4 报告、计划、协议、审计）
  - 旧根文件：anchors_n1n8.md、指标压缩稿.md、发现合并清单.md、字数核算_探针.txt、reviewer_check_v41_run431.py、watch_*.py
  - archive 原有内容保持不变（含旧泄漏审计、blind_scoring_v1-3、g2_v1_leaky）
- **冻结参照保留 `scorer_frozen/`**：`release_v4/`（v4.0.0 发布包）+ `release_v4_candidate_v4.1/`（v4.1 候选）——specs 与未来复算引用其 hash/schema，属只读参照不归档。
- AGENTS.md 关键路径段已更新为新结构；历史结论引用标注"实际读取加 archive/ 前缀"。

### 结果
- 根目录：`AGENTS.md / archive / corpus / data / docs / scorer_frozen / scorer_v5 / 判爆扑台账 / 全流程计划.md`
- 全部旧实验可审计（archive/），全部活跃文档在 docs/v5/ + scorer_v5/，冻结参照独立（scorer_frozen/）。

---

## 2026-08-29 14:40（UTC+8）· 第二阶段 Step 3：建立统一指标规格文件

### 执行内容
- 建立 `scorer_v5/specs/`：19 个指标各一个 YAML（B01–N7，固定顺序），+ README.md（字段说明 + 文件索引 + 版本）。
- 每个 YAML 含计划规定的 10 字段：`metric_id` / `construct` / `semantic_definition` / `llm_tasks` / `deterministic_inputs` / `required_evidence` / `deduplication_rule` / `score_rule` / `abstain_rule` / `version`。
- **内容忠实镜像冻结 v4.0.0**（`scorer/release_v4/anchor_v4_正式发布.md` §1–§19）：不改阈值、不改档位语义、不新增单篇例外。
- 校验（脚本）：19 文件 YAML 语法 OK、19 metric_id 集合 = 固定顺序、每文件 10 字段齐全、metric_id 与文件名一致。
- 版本：spec-draft-1（基线规格，Step 4 拆解前不改规则）。

### 关键设计
- **N6/B33/B01 等含 L/K/F 计算的指标**：`deterministic_inputs` 显式列出 L/K/N/F 由 Python 计算；LLM 只输出事件清单与语义判定，不再自算比例。
- **B18/B09/B16 等含比例/阈值/窗口的指标**：`deterministic_inputs` 列出比例、阈值、窗口边界等确定性输入。
- 语义判断（冲突有效性、关系升级、身份反转、同一事件、状态改变、同一场景）留在 `llm_tasks`。

### 下一步
- 第二阶段 Step 4：逐个拆解 19 指标（问题 A/B/C：哪些给 Python、哪些给 LLM、哪些主观需拆解），产出拆解结论后细化本目录。

---

## 2026-08-29 14:10（UTC+8）· 第一阶段 Step 2：旧数据重新分类

### 执行内容
- 建立分类登记：`docs/旧数据重新分类登记_v1.md`（Legacy / Development / Unseen 三类）。
- **分类方案（用户确认：Development 只从 pilot 50 本选）**：
  - **Legacy**：G2 v1、G2 v2、v4.0 score（fresh50 v1/v2 输出）、v4.1 候选 + held-out、blind_scoring_v4、archive——只读复盘，不进新正式实验。
  - **Development（10 本，全部 ∈ pilot，∉ val/fresh50 v1/v2）**：
    - `276` 贫困生换命茶（S, 12026）
    - `073` 弹幕斗表姐（A, 37153）
    - `132` 各给52万留学（A, 33906）
    - `188` 老公拿死胎换女儿（A, 10481）
    - `467` 新婚次日被扇耳光（B, 34294）
    - `413` 未婚夫拿我做赌注（B, 13050）
    - `056` 穿拖鞋去相亲（C, 35310）
    - `145` 归来后发现她怀孕（C, 33700）
    - `236` 那年风起落花深（C, 23459）
    - `303` 全家人都想姐姐继承家业（小扑, 15589）
  - **Unseen**：暂不指定 ID，等 v5 Dev Gate 通过后从全库扣除 (pilot ∪ fresh50v1 ∪ fresh50v2 ∪ val ∪ Dev10) 正式划定，划定前不得触碰。
- 校验（脚本）：10/10 ∈ pilot、∉ val、∉ fresh50 v1/v2、corpus 文件存在、无重复。
- 纪律：Development 上的成功 ≠ 模型验证成功；不得把 Development 当 G3/正式效标证据。

### 数字
- pilot 50 / fresh50 v1 50 / fresh50 v2 50 / val 67（永不触碰）/ Development 10 / 全库 586。

---

## 2026-08-29 13:45（UTC+8）· 第一阶段 Step 1：旧实验最终定性（v5 计划）

### 结论（写死）

1. **G2 v1：FAIL。**
   - 位置：`g2_stability_v1/`，prereg SHA `4003113989cbbf1674dc59de628f0ef7cebb8a53897649a0023a4ba512ddc4e4`。
   - 输入：fresh50 v1（50 本 × 3 轮 × 19 项 = 2850 评分点）。
   - 结果：逐项通过 **0/19**（必须 19/19 才 PASS）；三轮全同率实际范围 **12%–94%**（B33=12% 最差、N6=20%、B18=50%，仅 B36=94% 但为退化全同）；κ² / ordinal α 普遍 ≤0.45；345 条高分歧 68% 相邻档、32% 跨两档。
   - 已由独立 reviewer 复核 PASS + Director 独立复算确认。

2. **G2 v2：FAIL。**
   - 位置：`g2_stability_v2/`，prereg SHA `bd0be644f06e6c755f989d92acced7f3e869cc1cf5a776717bfbc40f873f2cca`。
   - 输入：fresh50 v2（50 本 × 3 轮 × 19 项 = 2850 评分点，fan-in 150/150）。
   - 结果：逐项通过 **0/19**；三轮全同率实际范围 **12%–98%**（B33=12% 最差、N6=24%、B36=98%、B23=94%、B30=92%，高全同项均为退化全同）；κ² / ordinal α 普遍 ≤0.45；高分歧 68% 相邻档、32% 跨两档。
   - reviewer 独立复核（run 526）确认：prereg hash 一致、fanin hash gate 3/3、19 指标全部点估计与 bootstrap CI 在 1e-8 内一致、高分歧诊断逐字一致。
   - Director 裁定 `director_ruling_per_bin_v1.md`（2026-08-29 03:35）：采纳"per_bin 门仅评估 eligible bins"解读（解读 A），对最终 FAIL 0/19 零影响。

3. **G2 v2 实际使用的是 v4.0.0，不是 v4.1。**
   - 证据：fresh50 v2 staging 每卡 `input/query.txt` 引用 `anchor-v4-formal-20260828`（v4.0.0 发布包），全文不含 "v4.1"；G2 v2 输入 = fanin merged（150 条）来自该 staging。
   - 因此**不得写"v4.1 正式 G2 失败"**。

4. **v4.1 当前仅通过规则/构造型 held-out 检查，没有完成真实长篇模型验证。**
   - v4.1 候选：`scorer/release_v4_candidate_v4.1/`（PASS，reviewer 57/57 分歧例一致）。
   - v4.1 held-out：`anchor_v4_heldout_v3/batch01-08/`（152 单元，8/8 PASS）。
   - 从未进入正式 fresh50 / G2 / G3 评分链。

5. **当前 v4.0 评分器存在已确认的执行问题**（以下逐条来自磁盘真实产物，见下方"定量证据"）：
   - **LLM 同时承担语义判断、计数、计算、阈值判断和最终打分**：19 指标单次处理，LLM 输出 evidence+reason+score，无 Python 确定性执行层。
   - **N6/B33 等指标存在 LLM 无法可靠取得 K 却自行估计的问题**：2850 条评分中，**reason 内**含 "L=/K=/F= 数值"（LLM 自行计算）共 **158 条**（N6=75、B33=82、B02=1）；含 "K≈/L≈/F≈/粗估/估算/无可用/环境无"（reason+evidence 合并口径）共 **283 条**（N6=122、B33=128）。显式自估句（"因环境无可用terminal精确跑L/K脚本，F为按正文逐段粗估，存在±0.05误差"）实际只出现 **1 条**（N6）。<br>*注：口径差异——只统计 reason 得 158，合并 evidence 后得 283；日志初稿误写 316 系口径错误，已按可复现口径修正。*
   - **存在"reason 判断为 5/10，但 score 填成 0/5"的内部矛盾**：reason 明确断言分值 ≠ 实际 score 共 **36 条**（B33=12、N6=8、B18=3、C14=3、N3=2、C22=2、B08=1、B03=1、B34=1、N7=1、B36=1、B01=1）；单条 reason 内出现两个不同分值断言 **5 条**。
   - **原始 JSON 有修复后才能通过的情况**：150 卡 candidate 经真实 `extract_card` 全链重建分类——**strict 解析 120 卡、smart_fix 修复 28 卡、permissive salvage 2 卡**（rep3/034、rep3/037）。其中 034/037 连 `_smart_fix` 都无法从 stdout 恢复（evidence 内嵌 `re.sub(r"\s+","",text)` 代码导致 `Invalid \escape`），经 permissive salvage 路径发布。fan-in 的 `_reject_dup` 拒绝后 merged 0 duplicate，但原始 stdout 存在需修复场景。<br>*注：初稿"148 卡需修复"系将 provenance 命中数 148 误当作解析修复数，已按 reviewer 复算（strict 120 + fix 28 + salvage 2）修正。*
   - **存在 duplicate key**：fan-in 与 qa_validate 使用 `object_pairs_hook=_reject_dup` 拒绝重复键（fan_in_merge.py:43、qa_validate.py:52、fan_in_fresh50_v2.py:50），说明历史上出现过该问题（当前 merged 0 dup 是因为 fan-in 已拒绝）。
   - **validator 只检查格式，不检查推理与 score 是否一致**：`qa_validate.py` 仅检查 schema（score∈{0,5,10}、evidence/reason 非空）、词法 token 数、指标顺序/集合、(rep,id) 唯一、sha256；**无任何"reason 语义与 score 一致性"检查**。上述 36 条矛盾即因此未被拦截。
   - **正式 G2 存在 provider/route 混用**：统一 provenance registry（`fresh50_v2_provenance_registry.json`）150/150 显示 provider 分布 minimax-cn 59 卡 + commandcode 91 卡（model 分布 MiniMax-M3=59、MiniMaxAI/MiniMax-M3=63、minimax/minimax-m3-free=28）；正式评分模型固定约束为 minimax-cn/MiniMax-M3，实际执行因额度切换混用 commandcode 池。
   - **没有从当前项目材料证明 temperature/top_p/seed 被完全固定**：盲评 CLI `cli_score_card.sh` 无 temperature/top_p/seed 参数；环境重置复测计划 v1（docs/环境重置与v3.1复测计划_v1.md:84,110,202）明确要求"执行前由 executor 从实际 provider 能力读回并冻结，不支持则 manifest 明记 unsupported"，但项目材料中无该冻结证据。
   - **19 指标+完整长文单次处理负担过重**：单条 candidate（19 指标 evidence+reason 全文）字符量 min 2764 / median 5965 / max 12664；>5000 字符 106/150、>8000 字符 30/150。单次模型调用需承载 19 项语义判断 + 全文定位 + 计算 + 打分。

6. **v4.0 不再进入新的正式实验。**（第一阶段的处置决定，记录于此。）

### 定性方法说明

- 全部数据来自磁盘真实产物：
  - G2 v1/v2 报告与 prereg SHA：`g2_stability_v1/results/g2_report_v1.md`、`g2_stability_v2/results/g2_report_v2.md`、prereg `.sha256`。
  - 评分数据：`fresh50_scoring_v2/fanin/full/merged.jsonl`（150 条，fan-in 已冻结）。
  - staging 锚点：`fresh50_scoring_v2/staging_root/rep1/001/input/query.txt`（v4.0.0 模板，无 v4.1）。
  - 溯源：`fresh50_scoring_v2/audit/fresh50_v2_provenance_registry.json`（150/150，0 unverified）。
  - 校验器：`盲评v3_1/scripts/qa_validate.py`、`fresh50_scoring_v2/scripts/fan_in_fresh50_v2.py`。
  - CLI：`盲评v3_1/scripts/cli_score_card.sh`。
- 定量检查脚本复现命令（可复现）：
  ```bash
  cd "D:/AI/workspace/小说训练"
  python -c "
  import json,re
  rows=[json.loads(l) for l in open('fresh50_scoring_v2/fanin/full/merged.jsonl',encoding='utf-8') if l.strip()]
  pat=re.compile(r'(?:落档|落|记|得|应(?:该|为|是|得)?|打|判|给)([0-9]+)\s*分')
  c=0
  for o in rows:
      for it in o['scores']:
          h=[int(x) for x in pat.findall(it.get('reason','')) if int(x) in (0,5,10)]
          if any(x!=it['score'] for x in h): c+=1
  print('reason-score 矛盾:', c)
  "
  ```
- 结论中"36 条矛盾 / 158 条自算（reason 口径）/ 283 条自估（reason+evidence 合并口径）/ strict 120 + fix 28 + salvage 2 / 字符分布"等数字来自上述 merged.jsonl 的实际统计，且已经独立 reviewer 逐条复算确认（reviewer run，2026-08-29，PASS with corrections）。
- **数字勘误记录（2026-08-29 14:20，依据 reviewer 复核）**：
  1. "316 条自估"→ 修正为 **283 条**（reason+evidence 合并口径，K≈/L≈/F≈/粗估/估算/无可用/环境无；N6=122、B33=128）；显式自估句仅 1 条（N6）。初稿 316 无支撑。
  2. "148 卡需 smart_fix"→ 修正为 **strict 120 + smart_fix 28 + salvage 2**。初稿 148 系把 provenance 命中数（148/150 直连 rc=0 attempt）误当解析修复数。
  3. "三轮全同率普遍 46–84%"→ 修正为 **v1 实际 12%–94%、v2 实际 12%–98%**（B33=12%/N6=24% 等极端低值此前被漏掉，低估了最差指标不稳定性）。
  4. "158 条自算"保留但补口径说明：仅 reason 内统计得 158（N6=75、B33=82、B02=1）；若含 evidence 则 287。
  - 以上修正均为纯文档勘误，不改变任何结论方向（G2 双 FAIL、v4.0 封存、v4.0 执行问题全部成立）。

### 影响与处置

- 本 Step 为"历史记录冻结"，**不修复任何东西**。
- 后续阶段（v5 架构）按《小说商业规律研究与评分器重构完整实施计划 v5》Step 2 起推进。
- v4.0 / v4.1 / fresh50 v1/v2 / G2 v1/v2 全部归入 **Legacy**（复盘用），不再进入新正式实验。
- 本日志新增为唯一日志文件；AGENTS.md 的"当前状态"段已同步更新为第一阶段封存状态。